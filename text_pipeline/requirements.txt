# Direct dependencies. Versions are what .venv currently has installed.
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
sse-starlette==3.4.8
regex==2026.7.19
