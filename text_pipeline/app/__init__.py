"""A from-scratch BPE tokenizer and the SSE endpoint that narrates it.

This file exists so `app` is a regular package rather than an implicit
namespace package. Nothing needs to be exported from here — import the
facade directly:

    from app.tokenizer import Tokenizer
"""
